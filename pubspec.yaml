name: npf_quick_response

description: NPF Quick Response App

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

  encrypt: ^5.0.3
  http: ^1.2.0
  connectivity_plus: ^5.0.2
  geocoding: ^2.1.0

  # 📍 GPS Location
  geolocator: ^10.1.1

  # 📷 Camera & Gallery
  image_picker: ^1.0.7

  # ⏱️ Timestamp formatting
  intl: ^0.19.0

  # 💾 OFFLINE STORAGE (Option H)
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # 📂 File system access (image paths)
  path_provider: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1

  # 🧠 Hive code generation
  build_runner: ^2.4.8
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true
